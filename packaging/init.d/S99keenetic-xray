#!/opt/bin/sh
# Entware rc.func-based init script: daemonization and the pidfile come
# from rc.func for free, so `daemon` just runs in the foreground.
#
# Verified on real router hardware (aarch64 Keenetic/Entware): `start`
# succeeds and the daemon process stays running.

ENABLED=yes
PROCS=keenetic-xray
ARGS="daemon"
PREARGS=""
DESC=$PROCS
PATH=/opt/sbin:/opt/bin:/opt/usr/sbin:/opt/usr/bin:/usr/sbin:/usr/bin:/sbin:/bin

# shellcheck source=/opt/etc/init.d/rc.func
. /opt/etc/init.d/rc.func
